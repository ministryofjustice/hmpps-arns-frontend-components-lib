import { ResponseDto } from './ResponseDto'

export interface OtherRoshRisksDto {
  escapeOrAbscond?: `${ResponseDto}`
  controlIssuesDisruptiveBehaviour?: `${ResponseDto}`
  breachOfTrust?: `${ResponseDto}`
  riskToOtherPrisoners?: `${ResponseDto}`
  assessedOn?: string
}
