describe('placeholder describe', () => {
  beforeEach(() => {})

  it('should run placeholder test', () => {
    // Arrange
    const expectedValue = 10
    // Act
    const actualValue = 5 + 5
    // Assert
    expect(actualValue).toBe(expectedValue)
  })
})
